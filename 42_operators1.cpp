#include<iostream>
using namespace std;
int main(){
    int a,b;
    cout<<"Enter the value of a:";
    cin>>a;
    cout<<"Enter a value of b :";
    cin>>b;

    //if (a>b)
   // if(a==b)
   //if (a<b)
  // if (a<=b)
  if (a!=b)
    cout<<"Yes";
    else
    cout<<"No";
}